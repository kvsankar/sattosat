[project]
name = "sattosat-verify"
version = "0.1.0"
description = "Verification scripts for SatToSat conjunction calculations"
requires-python = ">=3.10"
dependencies = [
    "sgp4>=2.22",
    "numpy>=1.24",
    "requests>=2.31",
    "python-dotenv>=1.0",
]
